<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.computerly">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".advice"></activity>
        <activity android:name=".io_devices" />
        <activity android:name=".speedup_system" />
        <activity android:name=".error_understanding" />
        <activity android:name=".window_installation" />
        <activity android:name=".basic_learning" />
        <activity android:name=".shortcut_keys" />
        <activity android:name=".software_info" />
        <activity android:name=".detailActivity" />
        <activity android:name=".hardware" />
        <activity android:name=".MainActivity" />
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>